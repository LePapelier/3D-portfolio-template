/**
 * Personal identity data — the one place to update your name and URLs.
 */

/** Displayed on the central node of the graph. */
export const fullName = 'Your Name'

/** Your GitHub profile URL — used as the href on the GitHub node. */
export const githubUrl = 'https://github.com/your-username'

/** Public URL of this portfolio — used in card detail links. */
export const siteUrl = 'https://your-portfolio.example.com'
